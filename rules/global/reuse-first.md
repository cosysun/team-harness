---
description: 先复用再自建，避免重复造轮子
alwaysApply: true
---

# Reuse First（全局规则）

## 核心原则

1. **技能优先**
   - 当用户提出需求时，先调用 `find-skills` 查找是否已有可复用 skill。
   - 只有在 `find-skills` 明确找不到合适能力时，才进入 `create-skill` 流程。

2. **开源优先**
   - 在讨论实施方案时，先调研是否已有成熟开源项目/方案可直接复用或二次封装。
   - 仅在无合适开源方案，或开源方案无法满足关键约束（如许可、性能、安全、维护性）时，才选择自研。

## 执行要求

- 明确说明“复用候选项”与“放弃复用的原因”。
- 默认以“集成/扩展已有方案”为首选路径，而非从零实现。
- 目标是减少重复造轮子，提升交付速度与稳定性。
