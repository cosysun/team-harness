## Python 项目架构

- 推荐 `src/` 布局或项目既有结构；保持模块边界清晰。
- 使用类型注解；格式化与 lint 由 `ruff` / `black` 负责（见 pre-commit）。

## 常用命令

- 安装依赖：`poetry install` 或 `pip install -r requirements.txt`
- 测试：`pytest`
- Lint/format：`ruff check .` / `ruff format .`
