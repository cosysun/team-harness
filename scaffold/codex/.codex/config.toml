# Team Harness — Codex project config (customize as needed)
# Docs: https://developers.openai.com/codex/config-basic

# Trust this repo to load project-local .codex/ settings
# approval_policy = "on-request"

# Optional: extra instruction filenames searched per directory (after AGENTS.md)
# project_doc_fallback_filenames = ["TEAM_GUIDE.md"]

# Default 32 KiB; increase if combined AGENTS.md + overrides grow large
# project_doc_max_bytes = 65536
