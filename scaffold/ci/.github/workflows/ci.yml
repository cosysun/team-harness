name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- Customize per stack (uncomment one block) ---

      # Go
      # - uses: actions/setup-go@v5
      #   with:
      #     go-version: "1.22"
      # - run: go test ./...

      # Python
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: "3.12"
      # - run: pip install -r requirements.txt && pytest

      # Node
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: "20"
      # - run: npm ci && npm test

      - name: Placeholder
        run: echo "Configure CI steps in .github/workflows/ci.yml"
